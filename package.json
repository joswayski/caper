{
  "name": "caper",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "node scripts/staging.mjs",
    "dev:web": "npm run dev --workspace @caper/web",
    "build": "npm run build:web",
    "build:web": "npm run build --workspace @caper/web",
    "start:web": "npm run start --workspace @caper/web",
    "check": "npm run build:web"
  },
  "engines": {
    "node": "24.x",
    "npm": ">=11"
  }
}
